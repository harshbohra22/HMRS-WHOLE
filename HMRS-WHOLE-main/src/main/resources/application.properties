spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driverClassName=org.h2.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

server.port=${PORT:8080}

# Optional email when application status changes (set spring.mail.host and app.mail.from to send real mail)
# spring.mail.host=smtp.example.com
# spring.mail.port=587
# spring.mail.username=
# spring.mail.password=
# app.mail.from=noreply@example.com

# Gemini (AI screening). Override with env GEMINI_API_KEY when you prefer not to commit a key.
gemini.api.key=AIzaSyB-X_z6Zz21-l5P08VWnEpSXcSgzTeArCI

# Spring AI - Gemini via OpenAI-compatible endpoint
spring.ai.openai.api-key=${GEMINI_API_KEY:${gemini.api.key}}
spring.ai.openai.base-url=https://generativelanguage.googleapis.com/v1beta/openai
spring.ai.openai.chat.options.model=gemini-2.0-flash
